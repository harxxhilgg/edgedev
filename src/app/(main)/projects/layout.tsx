import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Projects - My Work & Personal Projects",
};

export default function ProjectLayout({ children }: { children: React.ReactNode }) {
  return children;
};
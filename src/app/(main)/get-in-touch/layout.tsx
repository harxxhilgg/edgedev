import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact - Get in Touch",
};

export default function WorkExperienceLayout({ children }: { children: React.ReactNode }) {
  return children;
};